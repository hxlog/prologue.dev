# vercel
.vercel

# dependencies
/node_modules

# testing
/coverage

# next.js
/.next/

# local scratch (worktrees, one-off scripts)
/.tmp/

# yarn, not using zero-installs
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# env files (never commit secrets)
.env
.env.*
!.env.example